import React from "react";
import "./Header.css";

const Header = () => (
   <header className="header">
   	<h1>clickGame</h1>
   	<h2>Click on an image to earn points, but don't click on the smae one Twice!!!</h2>
   </header>
);
export default Header;